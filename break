步骤01 框架落地断点（更新于 2026-04-09 10:17:21 +0800）

当前状态
- 已在仓库根目录创建 `ai-quic/` 框架目录，并落地 `include/src/demo/interop/tests` 骨架。
- `ai-quic` 目录已补齐最小构建入口：`ai-quic/CMakeLists.txt`、`ai-quic/src/CMakeLists.txt`。
- 已新增最小公共接口与版本实现：`include/ai_quic/ai_quic.h`、`include/ai_quic/ai_quic_version.h`、`src/common/ai_quic_version.c`。
- `transport/tls/http3/congestion_control/demo/interop/tests` 均已创建 README 占位，用于后续逐步替换为真实实现。
- 已使用独立构建目录 `/tmp/ai-quic-build` 完成 `cmake configure + build`，确认骨架可编译成 `ai_quic_core` 静态库。

已完成
- 将“文档规划阶段”推进到“框架落地阶段”。
- 保持了 `docs` 约束、`xquic` 参考、`ai-quic` 落点三层边界，不在 `xquic/` 中写入新设计代码。
- 同步更新 `docs/plans/repo-file-hierarchy.md` 和 `docs/plans/process.md`，消除“尚未创建 `ai-quic`”的过期表述。

未完成
- `ai-quic` 仍为框架层，尚未实现步骤01所需的 Initial/Handshake/CRYPTO 真实协议逻辑。
- 尚未执行 `quic-interop-runner` 的 `handshake` 高保真验证。
- 尚未形成首条“下载成功且无 Retry”的可复现证据链。

下一步
1. 在 `ai-quic/src/transport` 实现最小包与帧收发链路（先 Long Header Initial/Handshake + CRYPTO/ACK）。
2. 在 `ai-quic/src/tls` 接入最小 TLS 驱动与密钥安装/丢弃时序。
3. 在 `ai-quic/demo` 打通最小下载闭环后，再接 `quic-interop-runner` 执行 `handshake`。

关键文件
- `ai-quic/CMakeLists.txt`
- `ai-quic/src/CMakeLists.txt`
- `ai-quic/include/ai_quic/ai_quic.h`
- `ai-quic/include/ai_quic/ai_quic_version.h`
- `ai-quic/src/common/ai_quic_version.c`
- `docs/plans/repo-file-hierarchy.md`
- `docs/plans/process.md`

待确认 / 风险
- 当前 `ai-quic` 版本实现仅为占位，不能代表协议能力。
- 后续进入握手实现时，`transport` 与 `tls` 的接口边界需要尽早定型，否则会带来反复重构成本。
- `docs/menu.md` 仍保留对 `docs/plans/plan-ai.md` 的历史引用；该文件当前不在工作树中，后续仍需清理。

如需用户执行
- none

最近验证
- `cmake -S ai-quic -B /tmp/ai-quic-build`
- `cmake --build /tmp/ai-quic-build`
